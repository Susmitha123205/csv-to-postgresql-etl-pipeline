# csv-to-postgresql-etl-pipeline
CSV to PostgreSQL ETL Pipeline — Built an end-to-end ETL pipeline using Python, Pandas, and PostgreSQL to process sales data from CSV files and load it into a relational database.
